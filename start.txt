# в корне проекта
docker compose up -d
npm run db:generate && npm run db:migrate
npm run dev        # терминал 1
npm run worker      # терминал 2

# в frontend/
cp .env.example .env
npm install
npm run dev         # терминал 3, откроется localhost:5173